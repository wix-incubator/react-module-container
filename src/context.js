import { createContext } from 'react';

/** @deprecated `ReactModuleContainerContext` exists as a workaround while transitioning from legacy features, please do not use */
export const ReactModuleContainerContext = createContext(null);
ReactModuleContainerContext.displayName = 'ReactModuleContainerContext(deprecated)';
